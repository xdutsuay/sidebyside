pub fn insertion_sort(arr: &mut [i32]) {
    for i in 1..arr.len() {
        let mut j = i;
        // Swap backwards until order is correct
        while j > 0 && input[j] < input[____________] {
            arr.swap(j, ____________);
            j -= 1;
        }
    }
}
